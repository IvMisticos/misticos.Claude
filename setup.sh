#!/usr/bin/env bash

echo "setup.sh is retired. Install the plugin instead:" >&2
echo "  claude plugin marketplace add IvMisticos/misticos.Claude && claude plugin install misticos@misticos --scope user" >&2
echo "Its session start hook applies the settings and, in the cloud environment, installs the tools." >&2
exit 1
