{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "command": "uv run \"${CURSOR_PLUGIN_ROOT}/hooks/reminder.py\" 1 1 --rules \"${CURSOR_PLUGIN_ROOT}/CLAUDE.md\" --context-from-size --output-shape cursor"
      }
    ],
    "postToolUse": [
      {
        "command": "uv run \"${CURSOR_PLUGIN_ROOT}/hooks/reminder.py\" 1 1 --rules \"${CURSOR_PLUGIN_ROOT}/CLAUDE.md\" --context-from-size --output-shape cursor"
      }
    ]
  }
}\n